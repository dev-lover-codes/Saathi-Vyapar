# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

.vercel
.env*

# Firebase Studio / Project IDX
.idx/

/Khata-mitr-main/
# AI instructions & blueprints
AGENTS.md
agents.md
CLAUDE.md
claude.md
GEMINI.md
gemini.md
blueprint.md
bluepront.md
.mcp.json
# Supabase CLI temp
supabase/.temp/
image.png
image copy.png
# Local screenshots/exports that may contain live credentials
whatsaap.png

# Python scratch from tooling
__pycache__/

# The env template carries no secrets and the README tells you to copy it.
# This must stay below every `.env*` rule above — there are two, and the
# second one silently re-ignored the file when the exception sat higher up.
!.env.local.example
